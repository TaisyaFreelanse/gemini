# 🎨 Посібник по роботі з Frontend

## 🌐 Доступ до системи

**Frontend:** http://localhost  
**API Documentation:** http://localhost:8000/docs  
**API Base URL:** http://localhost:8000/api/v1

---

## 📱 Структура сторінок

### 1. 📊 Dashboard (Головна сторінка)

**URL:** http://localhost/

**Функції:**
- ✅ Перегляд загальної статистики
- ✅ Запуск нового парсингу
- ✅ Моніторинг поточних сесій
- ✅ Перегляд швидкості обробки

**Як використовувати:**

1. **Запуск парсингу:**
   ```
   1. Введіть домени (по одному на рядок)
   2. Оберіть опції:
      - Use Proxy (використовувати proxy)
      - Use Cache (використовувати Redis кеш)
   3. Натисніть "Start Parsing"
   ```

2. **Моніторинг прогресу:**
   - Прогрес-бар показує % виконання
   - Лічильники: оброблено/успішно/помилки
   - Швидкість: domains/hour
   - Очікуваний час завершення

3. **Статистика:**
   - Всього сесій
   - Всього доменів оброблено
   - % успішності
   - Знайдено акцій

---

### 2. ⚙️ Configuration (Налаштування)

**URL:** http://localhost/configuration

**Функції:**
- ✅ Налаштування API для отримання доменів
- ✅ Налаштування Webhook
- ✅ Налаштування Proxy серверів
- ✅ Збереження конфігурації в БД

**Параметри:**

```yaml
Domains API:
  - URL для отримання списку доменів
  - Приклад: http://api.example.com/domains

Webhook:
  - URL: куди відправляти результати
  - Token: токен автентифікації
  - Приклад: https://webhook.site/your-unique-id

Proxy (опційно):
  - Host: адреса proxy сервера
  - HTTP Port: порт для HTTP (59100)
  - SOCKS Port: порт для SOCKS (59101)
  - Login/Password: credentials
```

**Як зберегти:**
1. Заповніть всі поля
2. Натисніть "Save Configuration"
3. Перевірте повідомлення про успіх

---

### 3. 📅 Scheduler (Планувальник)

**URL:** http://localhost/scheduler

**Функції:**
- ✅ Створення cron jobs
- ✅ Автоматичний запуск парсингу за розкладом
- ✅ Керування існуючими завданнями
- ✅ Перегляд історії виконання

**Формат Cron:**

```
┌───────────── minute (0-59)
│ ┌───────────── hour (0-23)
│ │ ┌───────────── day of month (1-31)
│ │ │ ┌───────────── month (1-12)
│ │ │ │ ┌───────────── day of week (0-6, Sunday=0)
│ │ │ │ │
* * * * *
```

**Приклади:**

| Розклад | Cron Expression | Опис |
|---------|-----------------|------|
| Кожну годину | `0 * * * *` | На початку кожної години |
| Щодня о 9:00 | `0 9 * * *` | Кожен день о 9:00 ранку |
| Кожні 30 хвилин | `*/30 * * * *` | Кожні 30 хвилин |
| По будням о 8:00 | `0 8 * * 1-5` | Пн-Пт о 8:00 |
| Кожну неділю | `0 0 * * 0` | Кожну неділю о 00:00 |

**Створення Job:**

1. Введіть назву: `Daily Parsing`
2. Введіть cron: `0 9 * * *`
3. Виберіть домени для парсингу
4. Натисніть "Create Job"

---

### 4. 📊 Reports (Звіти)

**URL:** http://localhost/reports

**Функції:**
- ✅ Перегляд всіх сесій парсингу
- ✅ Фільтрація по даті/статусу
- ✅ Експорт даних (CSV/JSON)
- ✅ Детальний перегляд результатів

**Типи звітів:**

1. **Загальна статистика:**
   - Всього сесій
   - Всього доменів
   - Успішність
   - Знайдено промокодів

2. **Список сесій:**
   - ID сесії
   - Дата/час запуску
   - Кількість доменів
   - Статус (running/completed/failed)
   - Прогрес

3. **Детальні результати:**
   - Домен
   - Статус скрапінгу
   - Знайдені акції
   - Промокоди
   - Помилки

**Експорт:**
- Натисніть "Export CSV" для завантаження
- Або "Export JSON" для API інтеграції

---

### 5. 📜 Logs (Логи)

**URL:** http://localhost/logs

**Функції:**
- ✅ Перегляд логів в реальному часі
- ✅ Фільтрація по рівню (INFO/WARNING/ERROR)
- ✅ Пошук по тексту
- ✅ Автооновлення

**Рівні логів:**

| Рівень | Колір | Опис |
|--------|-------|------|
| INFO | Синій | Інформаційні повідомлення |
| WARNING | Жовтий | Попередження |
| ERROR | Червоний | Помилки |
| DEBUG | Сірий | Детальна інформація |

---

## 🔌 API Endpoints (для розробників)

### Parsing

```bash
# Запустити парсинг
POST /api/v1/parsing/start
{
  "domains": ["example.com", "test.com"],
  "use_proxy": false,
  "use_cache": true
}

# Перевірити статус
GET /api/v1/parsing/status

# Зупинити парсинг
POST /api/v1/parsing/stop
```

### Configuration

```bash
# Отримати конфігурацію
GET /api/v1/config

# Оновити конфігурацію
PUT /api/v1/config
{
  "domains_api_url": "http://api.example.com/domains",
  "webhook_url": "https://webhook.site/unique-id",
  "webhook_token": "your_token",
  "use_proxy": false
}
```

### Reports

```bash
# Загальна статистика
GET /api/v1/reports/summary

# Список сесій
GET /api/v1/reports/sessions?limit=10&offset=0

# Список акцій
GET /api/v1/reports/deals?limit=50

# Експорт CSV
GET /api/v1/reports/export?format=csv
```

### Scheduler

```bash
# Статус планувальника
GET /api/v1/scheduler/status

# Список jobs
GET /api/v1/scheduler/jobs

# Створити cron job
POST /api/v1/scheduler/jobs/cron
{
  "name": "Daily Parsing",
  "schedule": "0 9 * * *",
  "domains": ["example.com"]
}

# Видалити job
DELETE /api/v1/scheduler/jobs/{job_id}
```

### Cache

```bash
# Статистика кешу
GET /api/v1/cache/stats

# Очистити кеш
DELETE /api/v1/cache/clear

# Видалити домен з кешу
DELETE /api/v1/cache/{domain}
```

---

## 🎯 Типові сценарії використання

### Сценарій 1: Разовий парсинг

1. Відкрийте http://localhost
2. Введіть список доменів
3. Увімкніть "Use Cache"
4. Натисніть "Start Parsing"
5. Очікуйте завершення
6. Перейдіть в "Reports" для перегляду результатів

### Сценарій 2: Автоматичний щоденний парсинг

1. Відкрийте http://localhost/scheduler
2. Натисніть "Create Job"
3. Введіть назву: "Daily Morning Parsing"
4. Введіть cron: `0 9 * * *` (щодня о 9:00)
5. Додайте домени
6. Збережіть job
7. Система автоматично запускатиме парсинг

### Сценарій 3: Налаштування Webhook

1. Створіть webhook на https://webhook.site
2. Скопіюйте унікальний URL
3. Відкрийте http://localhost/configuration
4. Вставте URL в поле "Webhook URL"
5. Додайте токен (опційно)
6. Збережіть
7. Всі результати будуть надсилатися на webhook

### Сценарій 4: Робота з Proxy

1. Отримайте доступ до proxy сервера
2. Відкрийте http://localhost/configuration
3. Заповніть:
   - Proxy Host: your-proxy.com
   - HTTP Port: 59100
   - SOCKS Port: 59101
   - Login: your_username
   - Password: your_password
4. Збережіть
5. При запуску парсингу увімкніть "Use Proxy"

---

## 🚀 Продуктивність

### Очікувана швидкість:

- **Без кешу:** ~400 domains/hour
- **З кешем (50% hit rate):** ~500-600 domains/hour
- **З кешем (80% hit rate):** ~700-800 domains/hour

### Факти що впливають на швидкість:

1. **Кеш Redis (+404x)**
   - Повторні запити миттєві
   - TTL: 1 година

2. **Proxy**
   - Може уповільнити на 20-30%
   - Але збільшує надійність

3. **Кількість Celery workers**
   - За замовчуванням: 10 workers
   - Можна збільшити в docker-compose.yml

4. **Швидкість Gemini API**
   - ~3 секунди на запит
   - Залежить від розміру HTML

---

## 🔧 Troubleshooting

### Проблема: Frontend не відкривається

```bash
# Перевірте статус контейнера
docker ps | grep frontend

# Перезапустіть frontend
docker-compose restart frontend

# Перегляньте логи
docker-compose logs frontend
```

### Проблема: Парсинг не запускається

```bash
# Перевірте Celery worker
docker ps | grep celery

# Перегляньте логи worker
docker-compose logs celery_worker

# Перезапустіть worker
docker-compose restart celery_worker
```

### Проблема: Не працює кеш

```bash
# Перевірте Redis
docker ps | grep redis

# Перевірте статистику
curl http://localhost:8000/api/v1/cache/stats

# Очистіть кеш
curl -X DELETE http://localhost:8000/api/v1/cache/clear
```

### Проблема: Повільний парсинг

1. Увімкніть кеш (Use Cache)
2. Збільште кількість workers в docker-compose.yml
3. Вимкніть proxy якщо не потрібен
4. Перевірте швидкість інтернету

---

## 📊 Моніторинг

### Метрики які потрібно відслідковувати:

1. **Швидкість парсингу**
   - Domains/hour > 150
   - Якщо менше - перевірте кеш і workers

2. **Відсоток успішності**
   - Success rate > 90%
   - Якщо менше - перевірте proxy і Gemini API

3. **Використання Redis**
   - Cache hit rate > 50%
   - Якщо менше - збільште TTL

4. **Помилки**
   - Перевіряйте логи щодня
   - Налаштуйте alerts на критичні помилки

---

## 🎉 Готово!

Система повністю працює і готова до використання!

**Основні посилання:**
- 🖥️ Frontend: http://localhost
- 📚 API Docs: http://localhost:8000/docs
- 📊 Cache Stats: http://localhost:8000/api/v1/cache/stats
- 📅 Scheduler: http://localhost/scheduler

**Документація:**
- `README.md` - Загальна інформація
- `TESTING.md` - Інструкції тестування
- `FRONTEND_GUIDE.md` - Цей документ
- `API_DOCUMENTATION.md` - Детальна документація API

**Підтримка:**
- GitHub Issues: створіть issue з описом проблеми
- Логи: `docker-compose logs -f`
- Статус: `docker ps`

---

**Успішної роботи! 🚀**
